- hosts: web

  tasks:
    - name: Remove old container
      shell: docker rm -f myapp || true

    - name: Run container
      shell: docker run -d -p 80:80 --name myapp -v $(pwd):/usr/local/apache2/htdocs/ httpd:2.4
